<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_card_white">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="22dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Tambah Data BMI"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#222222"
                android:layout_marginBottom="16dp" />

            <EditText
                android:id="@+id/edtBmiKategori"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:hint="Kategori"
                android:inputType="text"
                android:background="@drawable/bg_edittext"
                android:paddingHorizontal="14dp"
                android:layout_marginBottom="10dp" />

            <EditText
                android:id="@+id/edtBmiMin"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:hint="Min"
                android:inputType="numberDecimal"
                android:background="@drawable/bg_edittext"
                android:paddingHorizontal="14dp"
                android:layout_marginBottom="10dp" />

            <EditText
                android:id="@+id/edtBmiMax"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:hint="Max"
                android:inputType="numberDecimal"
                android:background="@drawable/bg_edittext"
                android:paddingHorizontal="14dp"
                android:layout_marginBottom="10dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/btnBMIBatal"
                    android:layout_width="0dp"
                    android:layout_height="50dp"
                    android:layout_weight="1"
                    android:gravity="center"
                    android:text="Batal"
                    android:textColor="#FFFFFF"
                    android:textStyle="bold"
                    android:background="@drawable/btn_merah"
                    android:layout_marginEnd="8dp" />

                <TextView
                    android:id="@+id/btnBMISimpan"
                    android:layout_width="0dp"
                    android:layout_height="50dp"
                    android:layout_weight="1"
                    android:gravity="center"
                    android:text="Simpan"
                    android:textColor="#FFFFFF"
                    android:textStyle="bold"
                    android:background="@drawable/btn_biru"
                    android:layout_marginStart="8dp" />
            </LinearLayout>

        </LinearLayout>
    </ScrollView>
</RelativeLayout>